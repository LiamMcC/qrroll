A query that will return all students from one class and at the same time 
return all the days the students were in or out for that class. 
Out will have to be not scanned.

A query that will return each students attendance as % per module

A query that will return a classes overall attendance across all modules

A query that will return an individual students attendance as a 1/5 or % of the course total. 
If possible showing a per module breakdown


A section to manually record a student who forgot their card.
For this one put Manual Entry into the DB instead of QR content.